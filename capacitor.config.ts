import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.margarita.anotador',
  appName: 'Anotador Margarita',
  webDir: 'dist/anotador'
};

export default config;
